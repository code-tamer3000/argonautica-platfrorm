---
name: work
description: Исполнение готовой задачи Linear по протоколу — от чтения задачи до зелёного PR и закрытия. Проверяет наличие протокола и калибратор размера, читает только нужные docs/, реализует строго в границах, проверяет через make lint/make test и Playwright, коммитит и обновляет статус в Linear. Используй, когда есть идентификатор задачи ARG-NN и надо её сделать.
---

# /work — задача Linear → зелёный PR

Вход — идентификатор задачи (`ARG-NN`). Если его не дали — спроси, не начинай по устному
пересказу: устный пересказ уже один раз потерял границы, ради этого весь протокол
и существует.

Прочитай [docs/TASK_PROTOCOL.md](../../../docs/TASK_PROTOCOL.md) — шаблон и калибратор.
Правила репозитория (команды, `make`, миграции, git) — в `CLAUDE.md`, они здесь главнее.

## Шаг 0. Два гейта до начала работы

`mcp__linear__get_issue` с `includeRelations: true`. Затем:

**Гейт протокола.** Нет разделов «Границы» и «Готово, когда» → остановись. Скажи:
задача не по протоколу, нужен `/idea`. Не додумывай границы сам — именно это додумывание
и превращает правку в двухчасовую сессию.

**Гейт размера.** Прогони калибратор. Не влезает, или стоит `> Требуется нарезка`, или
есть незакрытые `blockedBy` → остановись. Скажи: сначала `/slice ARG-NN` (или сначала
блокирующая задача). Не начинай «хотя бы кусочек».

Оба гейта — жёсткие. Пройти их «под ответственность пользователя» можно только если он
прямо на этом настаивает после того, как ты сказал о риске.

## Шаг 1. Завести работу

1. Статус → `In Progress` (`mcp__linear__save_issue`, `state: "In Progress"`).
2. Выбери режим по правилам git из `CLAUDE.md`:
   - **fix** — багфикс, мелкий рефактор, текст/стиль → коммит прямо в `develop`;
   - **feature** — новая фича, миграция, кросс-модульное изменение → ветка
     `feature/<slug>` (можно взять `gitBranchName` из задачи) + PR в `develop`.
   Не уверен, куда попадает — считай feature. Лишняя ветка дешевле, чем прямой коммит
     в `develop` не глядя.
3. Прочитай **только** доки, названные калибратором (≤ 2 из индекса `CLAUDE.md`).
   Не читай `docs/` целиком. Если задача трогает что-то, что выглядит переусложнённым, —
   сначала `docs/DECISIONS.md`, там может быть 🔒.

## Шаг 2. Сделать — строго в границах

- Реализуй ровно «Что сделать». Всё, что попадает в «Границы», не трогай, даже если
  оно рядом и «на пять минут».
- Заметил проблему за границами — не чини. Дособери в список и предложи в конце
  завести через `/idea`.
- Для fix: сначала регрессионный тест, который **падает** до правки, потом правка
  (правило из `CLAUDE.md`).
- Новый эндпоинт → интеграционный тест против реального тестового DB/Redis, без моков.
- Меняешь поведение, описанное в `docs/` → правь соответствующий док **в этой же задаче**.

## Шаг 3. Проверить до сдачи

Прогони то, что написано в «Как проверить», плюс обязательный минимум:

```
make lint && make test
```

Только через `make` — прямые `pytest`, `alembic`, `npm`, `vite` запрещены. Если цель падает
по причинам окружения, а не кода — остановись и доложи, не изобретай обходной путь.

Для UI-задач проверка глазами обязательна и не заменяется чтением диффа:

```
make local-up          # прод-подобный стек на https://localhost
```

Это настоящий `docker/docker-compose.prod.yml` локально — собранный SPA за nginx,
MinIO за тем же edge, один бэкенд без blue-green. Смотришь ровно то, что поедет:
прод-сборка фронта, прод-роуты nginx, прод-хосты presigned-URL. Хостового
`uvicorn --reload` + `vite dev` больше нет, и возвращать не надо — половина
медиа- и роутинг-багов ловится только на прод-топологии.

Пройди сценарий из «Как проверить» через Playwright MCP, проверь консоль на ошибки,
приложи скриншот к отчёту. Сертификат локально доверенный (`mkcert`), обхода
предупреждений браузера не требуется.

**Пустой стенд — не проверка.** На чистой БД любой экран показывает только пустое
состояние, и «посмотрел, всё нормально» здесь ничего не значит. Прежде чем смотреть,
залей данные (`backend/scripts/seed_demo_data.py` — разнородные демо-данные под
скриншоты) и убедись, что на экране есть то, что ты менял: карточки, бейджи, списки,
заполненные формы. Отдельно пройди краевые случаи из «Как проверить», если они там есть.

Не объявляй задачу сделанной, пока сценарий не прошёл. Тесты красные — так и говори,
с выводом.

## Шаг 4. Сдать

1. **Один** коммит в конце задачи, после зелёных `make lint && make test`.
   Conventional commits, по-русски в теле.
2. feature → PR в `develop`; при зелёном CI можно самомёржить. `develop` → `main`
   не трогай никогда, это делает человек руками.
3. В Linear: комментарий со ссылкой на PR/коммит + одна строка, что именно проверено;
   статус → `Done`.
4. Если по ходу выяснилось, что задача больше, чем казалась — **не дожимай**.
   Останови работу, закоммить то, что закончено и зелено, опиши в комментарии к задаче,
   что осталось, и заведи продолжение отдельной задачей. Работа «на грани переполнения
   контекста» — это дефект процесса, а не героизм.

## Шаг 5. Отчитаться

Коротко: что сделано, чем проверено (с фактическим результатом, а не «должно работать»),
ссылка на PR, что осталось за границами и достойно отдельной задачи.

## Границы скилла

- Не переформулируешь задачу. Считаешь, что задача не о том → останавливаешься и
  отправляешь в `/idea`.
- Не режешь задачу сам по ходу дела → `/slice`.
- Не трогаешь `docker/docker-compose.prod.yml`, `docker/deploy.sh`, `.env`,
  `docker/nginx/certs/` — никогда.
- Не закрываешь задачу, если проверка не прошла.
