# Активный цвет blue-green. ЕДИНСТВЕННЫЙ файл, который правит docker/deploy.sh
# (затем `nginx -s reload`). Не балансируем по обоим — трафик идёт строго на активный.
upstream backend {
    server backend-blue:8000;
}
